# Intergalactic Rave Survival Kit QR Prop-Art Artifact

Status: **verified scannable local artifact**
Task-board lane: `L. Rave Survival Kit / party safety edge`
Generated by: `scripts/build_survival_kit_qr_artifact.py`

## Files

- Poster/card PNG: `assets/survival-kit-qr/intergalactic-rave-survival-kit-card.png`
- Clean QR backup PNG: `assets/survival-kit-qr/intergalactic-rave-survival-kit-clean-qr.png`
- Exact QR payload: `assets/survival-kit-qr/intergalactic-rave-survival-kit-payload.txt`
- Verification manifest: `assets/survival-kit-qr/intergalactic-rave-survival-kit-manifest.json`

## What the QR contains

The QR encodes a short local-first Intergalactic DJs / DJ VANTA community-care checklist:

- water station visible;
- earplugs / listening breaks;
- buddy check + meet-up point;
- ask before touch/filming;
- chill zone + clear exits;
- sober human override ready;
- explicit scope: `community-care reminder only; not medical/legal/drug-use advice`.

## Why this matters for the hackathon demo

This turns the Rave Survival Kit from a feature note into a physical/digital show prop: judges can see that SonicForge Live is not only an AI DJ/VJ planner, but a local party operating system that includes care infrastructure, consent reminders, and human override language.

## Verification

Run:

```bash
PYTHONDONTWRITEBYTECODE=1 python3 scripts/build_survival_kit_qr_artifact.py
PYTHONDONTWRITEBYTECODE=1 python3 scripts/verify_survival_kit_qr_artifact.py
```

The verifier uses OpenCV `QRCodeDetector().detectAndDecode(...)` against both the clean QR and the final stylized poster. It fails unless both decode to the exact payload text.

## Closed gates

This artifact is local/demo-only by default:

- `starts_gpu: false`
- `starts_paid_api: false`
- `publishes_stream: false`
- `records_audio: false`
- `uploads_private_media: false`
- `requires_human_approval_before_public_use: true`

No ComfyUI `/prompt`, image model, public post, upload, stream, recording, or provider call is used to create this artifact. The QR is generated programmatically and composited with Pillow so it remains scannable instead of being hallucinated by an image model.

## Public-use note

Before printing or sharing publicly, an awake human should approve the payload destination and replace any local demo path with the intended safe public URL or event page. Do not add names, phone numbers, addresses, private room notes, photos, `.env` values, stream keys, or private media to the QR payload.
